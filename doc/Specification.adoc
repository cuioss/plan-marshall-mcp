= plan-marshall-mcp Specification
:toc: left
:toclevels: 3
:toc-title: Table of Contents
:sectnums:
:source-highlighter: highlight.js
:nofooter:

== Overview
_See Requirement link:Requirements.adoc#PM-ARCH-1[PM-ARCH-1: Process Ownership & Single Daemon Process]_

This document serves as the master technical specification index for *plan-marshall-mcp* (`PM-MCP`). While the link:Requirements.adoc[Requirements Document] specifies *what* the system must accomplish and the normative constraints it must satisfy, this specification tree defines *how* the system is designed, its internal architecture, data structures, wire protocols, and execution contracts.

For development sequencing and milestone delivery gates, see the link:roadmap.adoc[Roadmap].

== Specification Lifecycle Governance

Every component specification in `doc/specification/` declares its lifecycle status immediately following the traceability block using the following standard indicators:

[cols="1,2,3"]
|===
|Status |Meaning |Criteria for Transition

|`Status: PLANNED`
|Pre-implementation technical blueprint.
|Complete architecture, exact schemas, wire protocols, edge cases, and backtracking links to `Requirements.adoc` are defined. No code yet written.

|`Status: IN PROGRESS`
|Active implementation and testing underway.
|Implementation tasks in progress; partial code committed; contract being actively exercised by unit or integration tests.

|`Status: IMPLEMENTED`
|Complete and verified implementation.
|Implementation complete, 100% of acceptance criteria verified, passing integration test suite, and bidirectional code links (`file://...`) established.

|`Status: DEPRECATED`
|Superseded or retired component.
|Replaced by a newer specification or made obsolete by an architectural shift; migration notes provided.
|===

== Technical Specification Index

The implementation of `plan-marshall-mcp` is modularized into nine targeted technical specifications:

[cols="2,1,2,3"]
|===
|Specification Document |Status |Target Requirements |Technical Scope

|link:specification/daemon-endpoint.adoc[daemon-endpoint.adoc]
|`PLANNED`
|`PM-ARCH-2`, `PM-ARCH-3`, `PM-SEC-4`, `PM-CRED-1`, `PM-DIST-2`
|Loopback HTTP endpoint (`127.0.0.1`), per-start cookie authentication, Origin/Host validation, authenticated `/clients` bundle install endpoint, stdio relay mode (`pm-mcp serve --stdio`).

|link:specification/mcp-tools.adoc[mcp-tools.adoc]
|`PLANNED`
|`PM-TOOL-1`..`PM-TOOL-5`, `PM-SKILL-3`, `PM-SVC-1`..`PM-SVC-3`
|Closed JSON Schemas for `pm_start`, `pm_state`, `pm_do`, `pm_build`, `pm_lsp`, URI scheme contracts (`pm://plan/...`, `pm://epic/...`, `pm://skill/...`), and closed outcome envelopes.

|link:specification/hypermedia-format.adoc[hypermedia-format.adoc]
|`PLANNED`
|`PM-WF-1`..`PM-WF-4`, `PM-WF-8`, `PM-WF-9`, `PM-IMPL-3`
|TOON state representation grammar, link types (`submit`, `wait`, `dispatch`, `escalate`, `noop`), closed relation grammar, and composite wait expressions.

|link:specification/workflow-dsl.adoc[workflow-dsl.adoc]
|`PLANNED`
|`PM-WF-5`, `PM-WF-7`, `PM-IMPL-1`, `PM-IMPL-5`
|Formal EBNF grammar, AST data structures, guard and transition expression evaluation, primitive outcome routing, and state machine validation.

|link:specification/filesystem-layout.adoc[filesystem-layout.adoc]
|`PLANNED`
|`PM-ARCH-4`, `PM-ARCH-5`, `PM-ARCH-7`, `PM-MIG-2`, `PM-CRED-3`
|Repository workspace layout (`.plan/`, `.plan/plans/<plan_id>/mcp/`, `.plan/orchestrator/<epic_id>/mcp/`), worktree path resolution, and machine-global directories (`~/.plan-marshall/mcp/`, `~/.plan-marshall/credentials/`).

|link:specification/store-schemas.adoc[store-schemas.adoc]
|`PLANNED`
|`PM-ARCH-6`, `PM-IMPL-2`, `PM-MIG-3`, `PM-EXEC-2`
|Normative JSON and JSONL schemas for all persisted stores (`status.json`, `entry.json`, `findings.jsonl`, `tasks`, `enrolment.json`, `machine-config.json`), mandatory `format_version` envelopes, and Python lockfile coexistence.

|link:specification/move-journal.adoc[move-journal.adoc]
|`PLANNED`
|`PM-ARCH-7`, `PM-ARCH-8`
|Write-ahead move journal schema (`move-journal.json`), multi-step atomic directory rename protocol across worktree creation/destruction, and idempotent crash recovery algorithms.

|link:specification/job-runtime.adoc[job-runtime.adoc]
|`PLANNED`
|`PM-EXEC-1`..`PM-EXEC-5`, `PM-SEC-2`, `PM-SEC-5`, `PM-TECH-2`, `PM-TECH-4`
|Subprocess execution lifecycle, verify-not-resolve SHA-256 digest validation, tool and Python interpreter pinning, OS process-group isolation, signal escalation, and native build output parsers.

|link:specification/client-profiles.adoc[client-profiles.adoc]
|`PLANNED`
|`PM-CLIENT-1`..`PM-CLIENT-5`, `PM-DIST-1`, `PM-DIST-4`
|Client profile JSON schema (`schema_version`), dynamic on-the-fly vocabulary translation engine, client bundle packaging (`manifest.json`), and 3-tier identity mismatch audit logging.
|===

== Traceability Requirements

To ensure complete, verifiable traceability across the project lifecycle:

1. *Backtracking from Specifications*: Every specification document must include an explicit `== Traceability` section listing the `Requirements.adoc` IDs it fulfills, with relative links to `../Requirements.adoc#PM-<DOMAIN>-<NUM>`.
2. *Forward Linking from Requirements*: Every requirement in `doc/requirements/` must cross-reference its governing technical specification(s).
3. *Implementation Traceability*: When transitioning from `PLANNED` to `IMPLEMENTED`, the specification must be updated with links to the corresponding production classes in `src/main/java/` and automated tests in `src/test/java/`.
