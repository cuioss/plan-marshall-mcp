= plan-marshall-mcp

== Status

image:https://github.com/cuioss/plan-marshall-mcp/actions/workflows/maven.yml/badge.svg[Java CI with Maven,link=https://github.com/cuioss/plan-marshall-mcp/actions/workflows/maven.yml]
image:http://img.shields.io/:license-apache-blue.svg[License,link=http://www.apache.org/licenses/LICENSE-2.0.html]
image:https://img.shields.io/maven-central/v/de.cuioss/plan-marshall-mcp.svg?label=Maven%20Central["Maven Central", link="https://central.sonatype.com/artifact/de.cuioss/plan-marshall-mcp"]

https://sonarcloud.io/summary/new_code?id=cuioss_plan-marshall-mcp[image:https://sonarcloud.io/api/project_badges/measure?project=cuioss_plan-marshall-mcp&metric=alert_status[Quality
Gate Status]]
image:https://sonarcloud.io/api/project_badges/measure?project=cuioss_plan-marshall-mcp&metric=ncloc[Lines of Code,link=https://sonarcloud.io/summary/new_code?id=cuioss_plan-marshall-mcp]
image:https://sonarcloud.io/api/project_badges/measure?project=cuioss_plan-marshall-mcp&metric=coverage[Coverage,link=https://sonarcloud.io/summary/new_code?id=cuioss_plan-marshall-mcp]


https://cuioss.github.io/plan-marshall-mcp/about.html[Generated Documentation on github-pages]

== What is it?

*plan-marshall-mcp* (PM-MCP) is a local MCP server that takes over the process logic
https://github.com/cuioss/plan-marshall[plan-marshall] implements today as a corpus of filesystem
skills plus locally executed Python scripts. The client asks the server for the state of a plan;
the answer is a representation of that state together with pre-fabricated next tool calls
(hypermedia-driven workflow). The model follows links and contributes judgment only; the server
owns the state machine.

The design is described in the xref:doc/concept/README.adoc[concept documents].

[NOTE]
====
The project is at its very beginning: the build currently produces a Quarkus application with a
single `hello` MCP tool, packaged as a JVM container image and verified by container-based
integration tests.
====

== Modules

[cols="1,3"]
|===
|Module |Content

|`plan-marshall-mcp`
|The Quarkus application: MCP server (Quarkiverse Quarkus MCP Server, Streamable HTTP at `/mcp`),
health checks on the management port (`9000`, `/q/health`), container image
(`src/main/docker/Dockerfile.jvm`).

|`integration-tests`
|Builds the image, starts it with Docker Compose and runs the `*IT` tests against the running
container. Never published.
|===

== Technology

Java 25, Quarkus (via `de.cuioss:cui-quarkus-parent`),
https://github.com/quarkiverse/quarkus-mcp-server[Quarkus MCP Server],
https://github.com/cuioss/cui-http[cui-http] and https://github.com/cuioss/TokenSheriff[TokenSheriff].
See xref:doc/concept/10-technology.adoc[Technology].

== Build

[source,bash]
----
# Build and unit tests
./mvnw clean install

# Container-based integration tests (requires Docker)
./mvnw clean verify -Pintegration-tests -pl integration-tests -am

# Pre-commit: license headers and OpenRewrite recipes - review and commit the resulting diff
./mvnw -Ppre-commit clean verify -DskipTests
----

Run the image manually:

[source,bash]
----
./mvnw package -pl plan-marshall-mcp -am -DskipTests
docker build -f plan-marshall-mcp/src/main/docker/Dockerfile.jvm -t plan-marshall-mcp:jvm plan-marshall-mcp
docker run --rm -p 8080:8080 -p 9000:9000 plan-marshall-mcp:jvm
curl http://localhost:9000/q/health
----

== Credentials

All secrets (GPG, Sonatype, Sonar, Pages, Release App) are managed at the
https://github.com/cuioss/cuioss-organization[cuioss organization level] and passed explicitly to the
reusable workflows. No repo-level secrets need to be configured.
